[package]
name = "ue3-config-parser"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
regex = "1.5"
once_cell = "1.8"

[dev-dependencies]
expect-test = "1.1"
walkdir = "2"